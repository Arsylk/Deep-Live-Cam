# DroidCam uses the dedicated endpoint supplied by the shared v4l2loopback
# module. Some distro packages leave v4l2loopback-dc in modules-load.d even
# when that optional fork is not installed. Silently satisfy that stale load
# request so the second module cannot allocate competing /dev/video nodes.
install v4l2loopback_dc /usr/bin/true
